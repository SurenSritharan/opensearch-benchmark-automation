{
  "description": "DLS M3 — Full identity + security_classification clause. user-m3 is mapped ONLY to test_identity_classification (backend_role: grp-finance-readers). Most complex filter in the Moderate tier — tenant + identity + classification nested bool.",
  "engine": "__ENGINE__",
  "params": { "corpus_size": "1m" },
  "log_level": "info",
  "steps": [
    {
      "dataset": "cohere-wiki-en-768",
      "scenario": "vector-search",
      "label": "dls-m3-identity-cls-k10",
      "params": {
        "query_k": 10,
        "step_username": "user-m3",
        "step_password": "BenchmarkACL-2024!"
      }
    }
  ]
}
