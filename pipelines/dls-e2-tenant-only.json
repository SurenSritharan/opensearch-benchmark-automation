{
  "description": "DLS E2 — Static tenant_id term. user-e2 is mapped ONLY to test_tenant_only. Measures cost of a single term filter on a field absent from corpus docs (all docs pass). Index must exist from a prior build run.",
  "engine": "__ENGINE__",
  "params": { "corpus_size": "1m" },
  "log_level": "info",
  "steps": [
    {
      "dataset": "cohere-wiki-en-768",
      "scenario": "vector-search",
      "label": "dls-e2-tenant-only-k10",
      "params": {
        "query_k": 10,
        "step_username": "user-e2",
        "step_password": "BenchmarkACL-2024!"
      }
    }
  ]
}
